export enum USER_TYPES {
  LEARNER = 'LEARNER',
  TEACHER = 'TEACHER',
  ADMIN = 'ADMIN',
  MANAGER = 'MANAGER',
}

export enum ACCOUNT_STATES {
  NEW = 'NEW',
  VERIFIED = 'VERIFIED',
  ARCHIEVED = 'ARCHIEVED',
}

export enum GENDER_TYPES {
  MALE = 'male',
  FEMALE = 'female',
  NOT_SPECIFIED = 'not_specified',
}
